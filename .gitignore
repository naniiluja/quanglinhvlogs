# Repo công khai: không bao giờ commit bí mật hay ảnh gốc
node_modules
dist
.env.local
.env
.env.*
!.env.example
.wrangler
*.log
.DS_Store

# Cấu hình Claude Code cục bộ của từng máy
.claude/settings.local.json

# Ảnh gốc chưa cắt, chỉ giữ bản đã cắt trong public/
assets-src/

# Nhạc nền có bản quyền (Nơi Này Có Anh, Sơn Tùng M-TP): chủ dự án tự cung cấp file, chỉ nằm trong bản deploy,
# không đưa lên repo công khai. Máy khác clone về sẽ thiếu file này, xem tech-stack.md.
public/audio/noi-nay-co-anh.m4a
