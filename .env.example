# Sao chép thành .env.local rồi điền giá trị thật (không commit .env.local).
# Chỉ publishable key được đặt ở đây; tuyệt đối không đặt secret key hay service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
